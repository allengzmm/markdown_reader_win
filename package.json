{
  "name": "md-reader-win",
  "private": true,
  "version": "1.0.0",
  "author": "Alan Zou worked with Codex GPT",
  "description": "Local Windows markdown reader with math rendering and PDF export",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"wait-on tcp:5173 && electron .\"",
    "build": "vite build",
    "start": "electron .",
    "test:e2e": "npm run build && playwright test",
    "dist:win": "npm run build && electron-builder --config electron-builder.config.cjs --win nsis",
    "dist:portable": "npm run build && electron-builder --config electron-builder.config.cjs --win portable"
  },
  "dependencies": {
    "katex": "^0.16.22",
    "markdown-it": "^14.1.0",
    "markdown-it-texmath": "^1.0.0",
    "mermaid": "^11.13.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@vitejs/plugin-react": "^5.0.4",
    "concurrently": "^9.2.1",
    "electron": "^37.2.3",
    "electron-builder": "^26.0.12",
    "vite": "^7.1.5",
    "wait-on": "^8.0.5"
  }
}
